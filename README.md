To continue this mini demo do the following

    npm i typescript --save-dev
    npm i @types/node --save-dev
    tsc --init

Then adjust tsconfig as needed, while also including `"include": ["src/**/*"]`